{
  "name": "ip_address_tracker",
  "version": "1.0.0",
  "description": "This is my practical project to build out IP Address Tracker app.",
  "source": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --no-optimize --dist-dir build",
    "deploy": "vercel --prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.6.2"
  },
  "dependencies": {
    "leaflet": "^1.8.0"
  }
}
